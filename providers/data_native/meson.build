data_native_provider = shared_module(
  'obi_provider_data_native',
  ['obi_provider_data_native_v0.c'],
  include_directories: inc,
  dependencies: deps,
  install: true,
  install_dir: provider_install_dir,
)

provider_modules += [data_native_provider]
